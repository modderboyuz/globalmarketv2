# Disable the strict lockfile check that Vercel’s CI runs with “pnpm install --frozen-lockfile”.
# This lets the build proceed even when package.json has minor differences.
frozen-lockfile=false
prefer-frozen-lockfile=false
